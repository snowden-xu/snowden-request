/**
 * snowden-request
 * 一个现代化的HTTP请求库，支持浏览器和Node.js环境
 *
 * 目前导出axios实例作为默认导出
 * 后续将基于axios进行二次封装，提供更多便捷的功能
 */

import axios from "axios";

// Export the axios instance for now
export default axios;
